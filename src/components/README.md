# Components Directory

Place reusable UI components here.

## Rules
- **Naming**: Component files and functions should use `PascalCase`.
- **Structure**: Group related components in subfolders if necessary.
- **Props**: Define props interfaces/types in `src/types` or colocated if small.
