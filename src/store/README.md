# Store Directory

Place Redux Toolkit configuration and slices here.

## Rules
- **Management**: Use Redux Toolkit.
- **Naming**:
    - Slices/Reducers: `camelCase`.
    - Action Types (if manual): `UPPER_SNAKE_CASE`.
