# Hooks Directory

Place custom React hooks here.

## Rules
- **Naming**: File names and functions must be `camelCase` and start with `use` (e.g., `useAuth.ts`).
- **Usage**: Ensure hooks follow the Rules of Hooks (call at top level only).
