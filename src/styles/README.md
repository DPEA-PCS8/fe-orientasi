# Styles Directory

Place global styles, themes, or shared style utilities here.

## Rules
- **Tech**: CSS-in-JS (Styled-Components or Emotion).
- **CSS Classes**: If using raw CSS/modules, use `kebab-case`.
