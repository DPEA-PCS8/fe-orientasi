# Utils Directory

Place helper functions and utility logic here.

## Rules
- **Naming**: Functions should be `camelCase`.
- **Purity**: Prefer pure functions where possible.
- **Logging**: Use structured logging libraries instead of raw `console.log`.
