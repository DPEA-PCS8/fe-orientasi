# Types Directory

Place TypeScript interfaces and type definitions here.

## Rules
- **Naming**: Interfaces and Types should use `PascalCase`.
- **Scope**: Use this for shared types (API contracts, global entities). Local component props can stay in the component file.
