# Pages Directory

Place top-level page components (views) here.

## Rules
- **Naming**: Component files should use `PascalCase`.
- **Routing**: These components usually correspond to a route definition.
